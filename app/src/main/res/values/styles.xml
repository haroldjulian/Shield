<resources>

    <!-- Base application theme. -->
    <!-- <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"> -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">   <!-- Colocando aca y no en manifest todo el proyecto hereda lo mismo  -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>  <!-- color del estatus bar -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
